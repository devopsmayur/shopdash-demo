#ShopDash
A minimal demo app.

## Setup
1. Backend: cd backend && pip install -r requirements.txt && python app.py
2. Frontend: cd frontend && npm install && npm run start
3. Terraform: cd infrastructure/terraform && terraform init && terraform apply
